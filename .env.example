# .env.example

# ------------------------
# Required Environment Variables
# ------------------------

# TMDB API read access token from TheMovieDB.
TMDB_READ_ACCESS_TOKEN=your_tmdb_read_access_token_here

# Discord Bot token.
DISCORD_TOKEN=your_discord_token_here

# Discord Guild (Server) ID.
DISCORD_GUILD_ID=your_discord_guild_id_here

# Open Router API key for AI-powered descriptions.
OPEN_ROUTER_API_KEY=your_open_router_api_key_here

# ------------------------
# Optional Environment Variables
# ------------------------

# Path or URL to the bot's avatar image. Defaults to assets/avatar.jpg if not set.
DISCORD_AVATAR=assets/avatar.jpg

# Path or URL to the bot's banner image. Defaults to assets/banner.jpg if not set.
DISCORD_BANNER=assets/banner.jpg

# Desired Discord username for the bot. Defaults to "DHBW-ASE" if not set.
DISCORD_USERNAME=DHBW-ASE

# Operating mode for loading environment files. Set to "prod" for production or "dev" for development. Defaults to "dev".
MODE=dev

# Maximum number of pages to query from the TMDB API. Must be a positive integer. Defaults to 15.
MAX_API_PAGES_COUNT=15

# Minimum vote count for filtering recommendations. Must be a natural number. Defaults to 100.
MIN_VOTE_COUNT=100
